{
  "name": "akai-inference-router-spin",
  "version": "1.0.0",
  "description": "Fermyon Spin router for multi-region AI inference on Akamai Cloud",
  "scripts": {
    "build": "webpack --config webpack.config.js && spin js2wasm -o dist/router.wasm dist/router.js",
    "lint": "tsc --noEmit",
    "test": "echo 'Use spin up for local testing'"
  },
  "dependencies": {
    "@fermyon/spin-sdk": "^0.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-loader": "^9.0.0",
    "typescript": "^5.0.0",
    "webpack": "^5.88.0",
    "webpack-cli": "^5.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
